---
export interface Props {
  lang: string;
  children?: any;
}

const { lang } = Astro.props;
---

<!-- 
  Content visibility is controlled ENTIRELY by the CSS in head.astro
  using the html[data-genkit-lang] attribute selector.
-->
<div class="lang-content" data-lang={lang}>
  <slot />
</div>