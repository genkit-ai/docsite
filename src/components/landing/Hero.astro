---
import Button from './Button.astro';
import Icon from './Icon.astro';
---

<section class="pt-8 px-6 text-center">
  <div class="max-w-4xl mx-auto">
    <div class="mt-12 sm:mt-20 mb-6 sm:mb-8">
      <!-- Mobile: Logo above text -->
      <div class="flex flex-col items-center gap-4 sm:hidden">
        <Icon name="genkit-logo" size="md" class="w-12 h-12" />
        <h1 class="text-5xl leading-tight tracking-tight hero-title font-sans text-center">
          The AI Framework for<br>
          Full-Stack Apps
        </h1>
      </div>
      
      <!-- Desktop: Logo inline -->
      <h1 class="hidden sm:block text-5xl lg:text-7xl leading-tight tracking-tight hero-title font-sans text-center">
        The AI Framework for<br>
        Full-Stack Apps
      </h1>
    </div>
    
    <p class="subtitle text-muted mb-12 max-w-2xl mx-auto text-sm">
      Built and used in production by Google, Genkit is an open-source library with tools to help you build AI-powered experiences
    </p>
    
    <div class="flex flex-col sm:flex-row gap-3 sm:gap-4 justify-center items-center">
      <a class="hero-btn hero-btn-primary inline-flex items-center px-8 py-4 text-base border rounded-full min-h-[3.5rem] font-medium" href="/docs/get-started">
        Get Started
      </a>
      <a class="hero-btn hero-btn-secondary inline-flex items-center px-8 py-4 text-base border rounded-full min-h-[3.5rem] font-light" href="https://github.com/firebase/genkit">
        View on Github
      </a>
    </div>
  </div>
</section>

<style>
  .hero-badge {
    border-color: var(--border-light);
    color: var(--text-muted);
  }
  
  .hero-title {
    color: var(--text-primary);
    font-weight: 500;
  }
  
  .subtitle {
    font-size: 1.25rem !important;
    line-height: 1.5;
  }
  
  .hero-btn-primary {
    background-color: var(--genkit-primary);
    border-color: var(--genkit-primary);
    color: black;
  }
  
  .hero-btn-primary:hover {
    background-color: var(--genkit-primary-hover);
  }
  
  .hero-btn-secondary {
    background-color: var(--surface-primary);
    color: var(--text-primary);
  }
  
  .hero-btn-secondary:hover {
    background-color: var(--surface-elevated);
  }
</style>

