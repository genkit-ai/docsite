---
import Button from './Button.astro';
import Icon from './Icon.astro';
---

<section class="pt-8 px-6 text-center">
  <div class="max-w-4xl mx-auto">
    <span class="mt-10 inline-flex items-center text-sm rounded-full font-mono font-light border hero-badge px-4 py-2">
      Built and used in production by Google
    </span>
    
    <div class="mt-12 sm:mt-20 mb-12 sm:mb-20">
      <!-- Mobile: Logo above text -->
      <div class="flex flex-col items-center gap-4 sm:hidden">
        <Icon name="genkit-logo" size="md" class="w-12 h-12" />
        <h1 class="text-5xl leading-tight tracking-tight hero-title font-sans text-center">
          One framework for<br>
          building, debugging, and<br>
          deploying AI-powered apps
        </h1>
      </div>
      
      <!-- Desktop: Logo inline -->
      <h1 class="hidden sm:block text-5xl lg:text-7xl leading-tight tracking-tight hero-title font-sans text-center">
        One <span class="inline-flex items-center mx-2 relative sm:top-1">
          <Icon name="genkit-logo" size="md" class="w-12 h-12 lg:w-16 lg:h-16" />
        </span> framework for<br>
        building, debugging, and<br>
        deploying AI-powered apps
      </h1>
    </div>
    
    <p class="caption text-muted mb-12 max-w-xl mx-auto">
      Genkit is an open-source framework for integrating LLMs and building real features like chat, RAG, and agents with clean structured APIs.
    </p>
    
    <div class="flex flex-col sm:flex-row gap-3 sm:gap-4 justify-center items-center">
      <Button variant="primary" href="/docs/get-started">
        Install Genkit
      </Button>
      <Button variant="secondary" href="https://github.com/firebase/genkit">
        View on Github
      </Button>
    </div>
  </div>
</section>

<style>
  .hero-badge {
    border-color: var(--border-light);
    color: var(--text-muted);
  }
  
  .hero-title {
    color: var(--text-primary);
  }
</style>

