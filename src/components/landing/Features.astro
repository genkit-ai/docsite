---
import Button from './Button.astro';
import Link from './Link.astro';

const features = [
  {
    title: 'Unified APIs for any model',
    body: 'Use GoogleAI, OpenAI, Claude, and Ollama through one SDK.',
    href: '/docs/models',
    icon: '/assets/unified_apis.svg'
  },
  {
    title: 'Composable workflows',
    body: 'Structure chat, RAG, tool use, and agents with built-in primitives.',
    href: '/docs/flows',
    icon: '/assets/composable_workflows.svg'
  },
  {
    title: 'Production-ready tools',
    body: 'Local dev, debugging UI, and deployment to Firebase, Cloud Run, or your stack.',
    href: '/docs/devtools',
    icon: '/assets/production_ready.svg'
  }
];
---

<section class="pt-4 sm:pt-8 px-4 text-center max-w-6xl mx-auto">
  <h2 class="section-title mb-3">Simplified AI Integration</h2>
  <p class="caption text-muted mb-4">The fastest way to build, test, and deploy real AI<br /> features into your apps.</p>

  <div class="grid md:grid-cols-3 gap-4 sm:gap-6 lg:gap-8 mt-4 sm:mt-8">
    {features.map((feature) => (
      <div class="rounded-xl p-4 sm:p-6 lg:p-8 xl:p-10 transition-colors text-left feature-card flex flex-col">
        <img src={feature.icon} alt={feature.title} class="w-full mb-4 lg:mb-6" />
        <h3 class="card-title text-muted mb-3 lg:mb-4">{feature.title}</h3>
        <p class="card-text text-muted mb-4 lg:mb-6 flex-grow">{feature.body}</p>
        <Link variant="card" href={feature.href}>
          <span class="text-sm sm:text-base lg:text-lg">Learn more →</span>
        </Link>
      </div>
    ))}
  </div>

  <div class="mt-8 sm:mt-12">
    <Button variant="primary" href="https://github.com/genkit-ai/genkit-by-example">
      See Example Apps
    </Button>
  </div>
</section>

<style>
  .feature-card {
    background-color: var(--surface-subtle);
    border: 1px solid var(--border-secondary);
  }
  
  .feature-card:hover {
    opacity: 0.8;
  }
</style>
