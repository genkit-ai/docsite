# Genkit framework tutorials — bug-bash + audit triage

Tracking file for the framework-tutorial bug bash and the follow-up full audit + fixes.
(This file is local/untracked — not part of the docs.)

- **Docs branch:** `integrate-framework-frontend-nav` → pushed to `chrisraygill/docsite`
- **Samples branch:** `add-quickstart-guide-samples` → pushed to `genkit-ai/samples`
- **Compiled:** 2026-06-04 · **Last updated:** 2026-06-04 (after fix passes 1–3 + middleware removal + samples alignment)
- **Correct Go reference pattern:** `backend-frameworks/gin.mdx` (pointer style)
- **Working sample projects:** `~/Projects/samples` (now pushed to the branch above)

**Source key:** `BB` = bug bash (tester in parens) · `INV` = follow-up audit/investigation
**Severity:** blocker · major · minor · nit
**Status:** ✅ FIXED · OPEN · VERIFY-FIRST · BLOCKED (external dep) · DECISION (product/UX call)

## Progress log
- **Fix pass 1:** safe mechanical bucket (C2, C12–C18, C22, S4, S5, X2–X5). Commit `6c77b5b`.
- **Fix pass 2 (samples + docs):** all full-stack app + backend samples aligned to the canonical flow;
  Next.js generics, Fastify scaffold, Remix/TanStack scaffolders, Go pointer pattern, django, react-vite,
  version pins, READMEs. Docs commit `dedc497`; samples commit `a49dc2b`.
- **Fix pass 3 (middleware removal — all languages):** removed `retry`/middleware from the backend snippet
  and every JS/Go/Dart backend tutorial + sample (framework middleware preserved). Docs `dedc497`+`21a0558`;
  samples `c10eb6f`. Also un-ignored the Dart Shelf sample source (`37db5e2`).
- **Build status:** docsite `astro build` green (327 pages, all internal links valid). Go samples `go build`
  pass; JS samples `tsc --noEmit` pass.

## STILL OPEN (everything else below is ✅ FIXED)
- **U1–U4** — UX/IA (selector state, card affordance, lang-switch scroll, "After you choose a stack"). DECISION.
- **N1–N4** — nits (hardcoded `:8080`, flask admonition style, fastapi duplicate command, echo tags/model id).
- **Flag (maintainer):** genkit package version scheme is inconsistent (`genkit`/`google-genai` 1.3x vs
  `@genkit-ai/fetch` 0.1.x, `@genkit-ai/middleware` 0.6.x). Pins set to published versions to unblock installs.
- **Flag:** TanStack `createFileRoute(...).server.handlers` form is doc-sourced, not end-to-end build-verified
  (excerpt has no installed deps).

---

## 1. Code correctness

| ID | Page | Sev | Status |
|----|------|-----|--------|
| C1 | express.mdx "Serving models" | blocker | ✅ section removed (was broken; not a quickstart concern). Also removed from shelf.mdx + the dangling models.mdx links. |
| C2 | express.mdx flow import | blocker | ✅ `'./bargainChefFlow.js'` |
| C3 | express.mdx "Serving models" install | major | ✅ resolved with C1 (section removed) |
| C4 | nextjs.mdx App Router | blocker | ✅ `streamFlow<typeof bargainChefFlow>` (shares backend types; tsc verified) |
| C5 | remix.mdx scaffolder | blocker | ✅ `create-react-router` (create-remix was deprecated) |
| C6 | remix.mdx fetchHandlers prefix | blocker | ✅ doc + sample consistent; splat route + `'/api/bargainChefFlow'` prefix verified |
| C7 | remix.mdx route types/layout | major | ✅ reconciled to React Router 7 (flatRoutes, `Route.LinksFunction`) |
| C8 | tanstack-start.mdx scaffolder | blocker | ✅ `@tanstack/cli create` (create-start was wrong pkg) |
| C9 | tanstack-start.mdx legacy API | blocker | ✅ current `@tanstack/react-start` server-route API |
| C10 | tanstack-start.mdx prefix | blocker | ✅ resolved with the new route API |
| C11 | tanstack-start.mdx app/ vs src/ | major | ✅ doc + sample consistent on `app/` layout |
| C12 | hono.mdx flow import | blocker | ✅ `.js` extension |
| C13 | nestjs.mdx Request/Response | blocker | ✅ `import type` |
| C14 | nestjs.mdx Readable.fromWeb | blocker | ✅ typed cast |
| C15 | django.mdx SSE model_dump | blocker | ✅ `result.model_dump()` (doc + sample) |
| C16–C21 | chi/echo/nethttp Go streaming | major/minor | ✅ gin pointer pattern (compiles) + retry middleware removed |
| C22 | flask.mdx uv/pip | major | ✅ `uv add` / `uv run` |

## 2. Scaffold / setup

| ID | Page | Status |
|----|------|--------|
| S1–S3 | fastify.mdx | ✅ rewritten to the hand-rolled ESM setup the sample uses (+ typescript deps) |
| S4 | hono.mdx package list | ✅ completed bullets + scaffold note |
| S5 | sveltekit.mdx scaffold prompt | ✅ wizard wording |

## 3. Cross-cutting

| ID | Scope | Status |
|----|-------|--------|
| X1 | GitHub "finished code" links (all guides) | ✅ app links fixed to `/tree/main/` form; added the same-copy link to all 12 backend guides (incl. nethttp). Points to each tutorial subdir in genkit-ai/samples (assumes quickstarts merged to `main` before publish). |
| X2 | backend flow snippet "Angular component" comment | ✅ reworded + (snippet now middleware-free, identical to app snippet) |
| X3 | django/nestjs shipped TODOs | ✅ removed (no TODOs remain anywhere) |
| X4 | astro/react "Express integration" leftover | ✅ reworded |
| X5 | hono duplicate curl | ✅ removed |

## 4. UX / IA / platform (bug bash) — all DECISION / OPEN
- **U1** SDK selector "selected" state · **U2** FrameworkCard nav affordance · **U3** "After you choose a stack" placement · **U4** lang-switch scroll jump.

## 5. Nits
- **N1** hardcoded `:8080` (caveated) — OPEN
- **N2** flask `<Aside>` vs `:::tip` style — OPEN
- **N3** fastapi duplicate `genkit start` block — OPEN
- **N4** echo struct `jsonschema` tags / unpinned model id — OPEN
- **N5** astro doc/sample styles divergence — ✅ (sample now ships `public/styles.css`)

## 6. Found & fixed during the fix passes (not in original bug bash)
- ✅ **Middleware removed from the entire tutorial set** (JS/Go/Dart docs + samples + the backend snippet); framework middleware (echo/chi loggers, CORS) preserved.
- ✅ **Go value-type compile bug** introduced by fix-pass-1 (`value.Chunk != nil` on a value-type stream) — fixed by converting to the gin pointer pattern.
- ✅ **Dart Shelf sample source was gitignored** (repo-wide `bin/` rule) — un-ignored; the sample now ships its code.
- ✅ **Unpublished sample version pins** (`@genkit-ai/fetch`/`@genkit-ai/middleware` `1.35.0`) — set to published versions.
- ✅ **Every sample flow file is now byte-identical** to the canonical middleware-free, type-exporting snippet.
- ✅ **Sample READMEs** (stale `frontends/`/`backends/` paths, absolute paths, doc URLs) fixed across all samples.
- ✅ **genkit-cli is now a dev dependency** in every JS tutorial (Fastify/NestJS were installing it `-g`); no JS guide installs it globally.
- ✅ **net/http Go sample added** (`backend-frameworks/go/nethttp`) — the guide existed without a sample; `go build`/`go vet` pass.
- ✅ **"Finished code on GitHub" links** added/fixed across all app + backend guides (`/tree/main/` form).
- ✅ **Mashed bullets/imports/headings** from the middleware-removal edits cleaned up in express/hono/fastify/nestjs.

## Appendix: resolved before this triage (do not re-file)
- SvelteKit `.md` export (pageSource leak) — fixed by inlining. Hono/SvelteKit shipped TODOs — fixed.
  App-frameworks snippet "Angular component" comment — fixed. `.md`-export hazard class — resolved.
